# SnapSquad Backend - Python Dependencies
# Server is a BLIND COORDINATOR. Zero ML models.
# Detection (SCRFD) and embedding (MobileFaceNet) run ON DEVICE.
# Server only runs HDBSCAN clustering (pure math via numpy).

# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# Database (PostgreSQL)
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0
pgvector==0.4.2

# Validation and Config
pydantic==2.10.3
pydantic-settings==2.7.0

# Auth
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Background Jobs
redis==5.2.0
rq==1.16.2

# Clustering (pure math, no ML models)
numpy==2.1.3
hdbscan==0.8.40
scikit-learn>=1.3.0

# Cloud Storage (optional)
cloudinary>=1.36.0

# HTTP Client (Push Notifications)
httpx==0.27.0
